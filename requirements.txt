# Dependencies for the deployed Streamlit app (Project 1 - Recipe Manager App).
# Streamlit Community Cloud installs from this root file.
# Other projects in this repo keep their own requirements.txt in their own folder
# (see jewelry_search/requirements.txt) so their heavier dependencies are not
# installed here.

streamlit
pandas
requests
python-dotenv
openai
